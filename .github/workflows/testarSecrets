- name: Testar acesso aso secretes
on:
  schedule:
  workflow_dispatch: # Habilita execução manual
  run: |
    if [ -z "${{ secrets.GT_TOKEN }}" ]; then
      echo "GT_TOKEN não está configurado!"
      exit 1
    else
      echo "GT_TOKEN encontrado com sucesso!"
    fi
